import { loadProject } from './storage.js';
import { startListener, newCard } from './displayController.js';

// const todoList = loadProject();
// console.log(todoList);

// startListener();

newCard('Check it out 1', 'pink');
newCard('Check it out 2', 'lightblue');